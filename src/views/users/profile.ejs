<!DOCTYPE html>
<html lang="en">
<head>

    <%- include( "../partials/head" ) %>
    <link href="/css/profileStyle.css" rel="stylesheet"/>
    
</head>
<body>

    <%- include( "../partials/header" ) %>

    <main>

        <div class="pagina-titulo">
            <h1>Mi perfil</h1>
        </div>

        <section class="perfil">
                <div class="datos">
                    <div class="user-img">
                        <img src="<%=user.imagen%>">
                    </div>
                    <div class="user-info">
                        <h3><%=user.nombre%> <%=user.apellido%></h3> <a href=""><i class="fas fa-pen i-especial"></i></a>
                        <p> <span class="bold">Tipo:</span> <%=user.tipo%></p>
                        <p><i class="fas fa-envelope"></i> <%=user.email%></p>
                    </div>
                </div>
        </section>

        <section id="productos-parecidos">

            <h2>Tus favoritos</h2>

            <% for (let i = 0; i < 4; i++) { %>

            <article class="producto-parecido">
                
                <a href="/products/<%=productos[i].id%>">
                <div class="foto-parecido">
                    <img src="<%=productos[i].thumbnail%>" alt="<%=productos[i].alt%>">
                </div>
                <p class="titulo-producto"><%=productos[i].nombre%></p>
                <p class="descripcion-producto"><%=productos[i].desc%></p>
                <p class="precio-producto">$<%=productos[i].precio%></p>
                </a>
            
            </article>

            <% } %>
            
        </section>
        
    </main>

    <%- include( "../partials/footer" ) %>
    
</body>
</html>